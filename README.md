# Acceptance Letter — for Chotu

A reply to an Appointment Contract. Static site — `index.html`, `style.css`, `main.js`, `img/`.

Run locally:

```
python -m http.server 8765
```

Hosted via GitHub Pages from the `main` branch (root).
